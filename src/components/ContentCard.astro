---
interface Props {
  title: string;
  description: string;
  href: string;
  meta?: string;
  tags?: string[];
  lang?: string;
}

const { title, description, href, meta, tags = [], lang = "en" } = Astro.props;
---

<article class="card">
  {meta && <p class="meta">{meta}</p>}
  <h3><a href={href}>{title}</a></h3>
  <p>{description}</p>
  {
    tags.length > 0 && (
      <div class="tag-row">
        {tags.map((tag) => (
          <a class="tag" href={`/${lang}/tags/${encodeURIComponent(tag)}/`}>
            {tag}
          </a>
        ))}
      </div>
    )
  }
</article>
